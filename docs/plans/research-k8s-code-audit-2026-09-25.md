# K8s readiness code audit — `hippo serve`

Read-only audit, 2026-09-25. Repo: `C:/Users/skf_s/hippo`. No dedicated Helm chart
or Kubernetes manifests exist in this repo today — the only container reference is
`deploy/aml/` (a Fly.io evaluation deployment). EI10 ("Deployment tiers", ROADMAP.md:1331,
"[planned, 6-10w]") is where Helm/Terraform/Postgres packaging is scoped; nothing there
is built yet. This audit is what would need to change to go from "AML Fly deploy" to
"K8s Deployment + Helm chart + sidecar + CronJob".

Every row cites `file:line` with a verbatim quote from a read this session.

## 1. Server lifecycle

| finding | file:line | severity | suggested change |
|---|---|---|---|
| Binding to `0.0.0.0` (required for a pod to be reachable by other pods/kubelet) is refused unless `HIPPO_REQUIRE_AUTH=1` is also set — self-defending, but easy to miss in a chart. `if (!LOOPBACK_HOSTS.has(host) && process.env.HIPPO_REQUIRE_AUTH !== '1') { throw new Error(...) }` | `src/server.ts:3354-3360` | blocker | Chart must hard-wire both `--host 0.0.0.0` and `HIPPO_REQUIRE_AUTH=1`; document as a required pair, not optional hardening. |
| `serve` CLI case requires `.hippo` to already be initialized: `case 'serve': { requireInit(hippoRoot);` | `src/cli.ts:10434` | fix before chart | Chart needs an init step (initContainer or entrypoint script) running `hippo init` against the mounted volume before `hippo serve` starts. The existing `deploy/aml/entrypoint.sh:29-31` already does this (`if [ ! -f /data/.hippo/index.json ] ... ]; then node /app/dist/cli.js init --no-hooks --no-schedule --no-learn; fi`) — port this pattern into the chart's entrypoint/initContainer. |
| `--host`/`HIPPO_PORT` are CLI-flag/env driven; there is no `--require-auth` CLI flag, `HIPPO_REQUIRE_AUTH` is env-only and read deep in `server.ts`, not surfaced by the `serve` case. `const host = typeof flags['host'] === 'string' ? (flags['host'] as string) : '127.0.0.1';` … `const handle = await serve({ hippoRoot, port, host, handleSignals: true });` | `src/cli.ts:10433-10443` | nice-to-have | Fine via Helm `env:` block; note in chart docs so it isn't forgotten. |
| `gracefulShutdown` force-closes **all** connections (`server.closeAllConnections?.()`) before `server.close()` resolves — this is not a "stop accepting new conns, let in-flight finish" drain, it kills in-flight requests immediately. Added deliberately to unstick SSE streams (see comment at line 3474-3478). | `src/server.ts:3466-3499`, esp. `3478` (`server.closeAllConnections?.();`) and `3486-3499` (`gracefulShutdown` handler, `finally { process.exit(0); }`) | fix before chart | Acceptable given K8s's default 30s SIGTERM grace period mostly masks it, but a write request in flight at the instant of a rolling restart gets its socket destroyed rather than completing. If write-safety under rolling deploys matters, separate "stop accepting new connections" from the SSE-specific force-close. |
| `SIGTERM`/`SIGINT` are wired only when `serve()` is called with `handleSignals: true` (true from the CLI path): `process.once('SIGTERM', () => { void gracefulShutdown('SIGTERM'); }); process.once('SIGINT', () => { void gracefulShutdown('SIGINT'); });` | `src/server.ts:3498-3499` | informational | Matches K8s expectations (SIGTERM triggers shutdown, `terminationGracePeriodSeconds` gives it time). No change needed. |
| Pidfile `${hippoRoot}/server.pid` schema: `{schema, pid, port, url, started_at}`. | `src/server-detect.ts:17-20` (`const PIDFILE = 'server.pid';`), write logic `164-179` | informational | No change needed — noted for chart authors deciding whether `.hippo` sits on a PV. |
| **Stale pidfile / PID-reuse across a pod restart is handled correctly.** Detection is not a bare PID-exists check: it requires `process.kill(pid,0)` to succeed AND a live `GET /health` whose `started_at` matches the pidfile's `started_at`. A non-matching or refused probe unlinks the pidfile; a timeout (300ms) leaves the file in place but returns `null` (does not block a new `serve()`). Quote: `process.kill(info.pid, 0)` (`server-detect.ts:71-80`); `fetch(\`${info.url}/health\`...)`, `if (body.started_at !== info.started_at)` (`server-detect.ts:109-140`); timeout branch `if ((err as {name?:unknown})?.name !== 'TimeoutError') { unlinkSync(path); } return null;` (`server-detect.ts:141-151`) | `src/server-detect.ts:59-154` | informational | A fresh container's process won't answer `/health` with the old `started_at`, so the old pidfile is either unlinked or ignored and `serve()` proceeds to overwrite it. A new PID-1 that happens to reuse the stale PID number will fail the `/health` round-trip check (different `started_at`), so no false "already running." Worth one doc line for operators since it looks fragile on first read. |
| Concurrent-serve detection only throws when `detectServer` returns a live, health-probe-verified peer on the *same* `hippoRoot`: `const existing = await detectServer(opts.hippoRoot); if (existing) { throw new Error(\`hippo serve: already running on port ${existing.port} (pid ${existing.pid}). ...\`); }` | `src/server.ts:3362-3372` | informational | No false-positive crash-loop risk from container restarts. A genuine same-`hippoRoot` double-start (e.g. two containers racing on a shared volume) is correctly blocked. |

## 2. Health, readiness, metrics

| finding | file:line | severity | suggested change |
|---|---|---|---|
| `GET /health` is pure liveness: no DB open call, no schema check, no I/O. Loopback callers get `{ok, version, started_at, pid}`; non-loopback callers (i.e. every real kubelet probe, since kubelet hits the pod IP, not loopback) get `{ok:true}` only. `if (method === 'GET' && path === '/health') { ... if (isLoopback(req.socket.remoteAddress)) { sendJson(res, 200, {ok:true, version:VERSION, started_at:startedAt, pid:process.pid}); } else { sendJson(res, 200, {ok:true}); } return; }` | `src/server.ts:754-770` | fix before chart | Good as a K8s **liveness** probe (correctly has no downstream-dependency check). Use it for `livenessProbe`. |
| No separate readiness endpoint exists. Grep of `src/` for `/healthz`, `/ready`, `/live` returns no matches. A broken volume mount or a failed migration would still return `{ok:true}` from `/health`, marking the pod Ready and routing it traffic. | n/a (absence confirmed by grep across `src/`) | fix before chart | Add `GET /ready` (or a query flag on `/health`) that does a cheap DB round-trip (open/no-op query); wire as `readinessProbe`. Keep `/health` liveness-only. |
| No `/metrics` endpoint, no `prom-client` or any metrics registration anywhere in `src/`. Confirmed independently by ROADMAP.md itself: "**Pending:** retrieval-trace API ... per-tenant cost/usage rollups, **Prometheus exporter**, decay-curve telemetry" | grep of `src/` for `prom-client`/`/metrics` — no matches; `ROADMAP.md:240` | nice-to-have | Not required to run. A chart with a `ServiceMonitor`/scrape annotation has nothing to point at today — flag as a known gap, not a blocker for a first ship. |

## 3. Storage

| finding | file:line | severity | suggested change |
|---|---|---|---|
| Global store root resolves `$HIPPO_HOME` > `$XDG_DATA_HOME/hippo` > `~/.hippo`: `const hippoHome = process.env.HIPPO_HOME?.trim(); if (hippoHome) return hippoHome; const xdgData = process.env.XDG_DATA_HOME?.trim(); if (xdgData) return path.join(xdgData, 'hippo'); return path.join(os.homedir(), '.hippo');` | `src/project-identity.ts:196-202` (`src/shared.ts:33-37` `getGlobalRoot()` delegates) | blocker | Set `HIPPO_HOME` to the PVC mount path identically in the Deployment and the CronJob pod spec. A missing var on either one silently falls back to `~/.hippo` under that container's `$HOME` — a different, ephemeral store, with no error. |
| `hippo serve` operates on a distinct `hippoRoot` (project-local `.hippo/`) via `getHippoDbPath`, separate from the global `HIPPO_HOME` concept: `export function getHippoDbPath(hippoRoot: string): string { return path.join(hippoRoot, 'hippo.db'); }` | `src/db.ts:2501-2503` | fix before chart | Confirm the chart points the Deployment's `serve` and the CronJob's `sleep` at the *same* `hippoRoot`/cwd (or the same `HIPPO_HOME`), not just matching env var names. |
| WAL mode, busy_timeout, synchronous, checkpoint pragmas are all set on DB open: `db.exec('PRAGMA busy_timeout = 5000'); execWithBusyRetry(db, 'PRAGMA journal_mode = WAL'); db.exec('PRAGMA synchronous = NORMAL'); db.exec('PRAGMA wal_autocheckpoint = 100'); db.exec('PRAGMA foreign_keys = ON');` | `src/db.ts:2546-2550` | informational | Reasonable local-disk concurrency setup; no change needed. |
| **No advisory/flock layer anywhere in `src/`.** SQLite's WAL correctness for two OS processes depends entirely on the OS/filesystem giving correct `fcntl` byte-range locks on `.db`/`.db-wal`/`.db-shm`. The code assumes local-disk POSIX semantics; nothing guards against a network filesystem. Code comment acknowledging a related gap: "busy_timeout covers neither of this file's two contended statements: SQLite skips the busy handler for `PRAGMA journal_mode` and for a write that upgrades a deferred read snapshot." | `src/db.ts:2525-2540` (comment); no locking code found repo-wide | **blocker (infra, not code)** | Pin the PVC's `StorageClass` to a local-disk-backed, `ReadWriteOnce` volume (e.g. EBS via a node-local CSI driver). SQLite WAL mode is officially unsupported on NFS/EFS-class network filesystems — it can silently corrupt the DB rather than error. If `hippo sleep` is ever scaled to multiple concurrent pods, do not point them at the same file over a shared FS; that needs the HTTP-route approach below, which does not exist for cross-pod use yet. |
| `POST /v1/sleep` exists and is admin-gated, but is **loopback-only**, so a separate CronJob Pod (its own network namespace/IP) can never reach it: `if (!isLoopback(req.socket.remoteAddress)) { throw new HttpError(403, '/v1/sleep is loopback-only (host-wide consolidation; see CHANGELOG v1.11.4)'); } ... if (sleepCtx.actor.role !== 'admin') { throw new HttpError(403, '/v1/sleep requires admin role'); }`. `isLoopback` only accepts `127.0.0.1`/`::1`/`::ffff:127.0.0.1` — `src/server.ts:506-510`. The test suite documents this as intentional current-state, not a bug, and names the missing escape hatch: "today /v1/sleep is loopback-only... Future test: spawn serve with a non-loopback bind via HIPPO_BIND_ALL (when that env knob exists)" — `HIPPO_BIND_ALL` does not exist anywhere in `src/` (grep confirms). | `src/server.ts:1247-1263`; test note `tests/server-sleep-admin-gate.test.ts:4-8` | **blocker (for the CronJob-over-HTTP idea)** | `/v1/sleep` over HTTP only works today as an **in-pod sidecar cron** (e.g. supercronic calling `127.0.0.1:PORT/v1/sleep` inside the same Pod as `serve`, sharing the network namespace) — not as a separate K8s CronJob object, which is a different Pod by definition. Either build the non-loopback/API-key-gated `/v1/sleep` path (new work), or restructure the CronJob as an in-Deployment-Pod cron sidecar. |
| `hippo sleep` CLI never checks for a live server on the same root before opening the DB directly — no `detectServer()` guard, unlike `serve()`'s own boot-time check. `cmdSleepCore` opens the store directly via `api.sleep(ctx, ...)`. | `src/cli.ts:3144-3177` (no guard) vs. `src/server.ts:3362-3372` (`serve()`'s own guard) | fix before chart | If the CronJob's `hippo sleep` process and the Deployment's `hippo serve` process both hold `hippo.db` open on the same PVC, safety is mediated only by SQLite's own file locks — fine on local-disk storage, unsafe on network FS (see the WAL row above). Apply the same `detectServer`-style guard to the `sleep` CLI path, or explicitly document "concurrent serve + sleep on one PVC is supported only on local-disk storage classes" before shipping the chart. |
| Migrations run automatically on every DB open, under `BEGIN IMMEDIATE` with a re-check of `schema_version` before applying — correctly built for concurrent-startup races **given working POSIX locks**: `for (const migration of MIGRATIONS) { if (migration.version <= currentVersion) continue; execWithBusyRetry(db, 'BEGIN IMMEDIATE'); try { assertBinaryCompatible(db); const applied = getSchemaVersion(db); if (applied >= migration.version) { db.exec('COMMIT'); ... continue; } migration.up(db, { hippoRoot }); setSchemaVersion(db, migration.version); db.exec('COMMIT'); ...` | `src/db.ts:2571-2601` (called from `openHippoDb` at `2551`) | informational | Not a distinct risk beyond the WAL/locking row above — a second pod racing to start against a fresh empty PVC will not double-apply a migration on sane storage. Collapses along with everything else if the storage class has broken locking. |
| ROADMAP confirms this is a known, named constraint, not an oversight: "**SQLite allows one writer at a time:** fine per developer; for a company-wide server it is why A6 (Postgres) and EI10 exist." | `ROADMAP.md:1608` | informational | Matches the code-level findings above; A6/EI10 (Postgres backend) is the acknowledged structural fix, not yet built (see section 7). |

## 4. Embeddings

| finding | file:line | severity | suggested change |
|---|---|---|---|
| Default local model: `Xenova/all-MiniLM-L6-v2`, loaded via dynamic `import()` of `@huggingface/transformers` (fallback `@xenova/transformers`): `export const DEFAULT_EMBEDDING_MODEL = 'Xenova/all-MiniLM-L6-v2';` (`src/embeddings.ts:28`); `const instance = await pipelineFn('feature-extraction', model, { quantized });` (`src/embeddings.ts:220`) | `src/embeddings.ts:28, 182-232` | fix before chart | Document the default model id and footprint in the chart's values.yaml comments. |
| **`@huggingface/transformers`/`@xenova/transformers` are not real dependencies — declared only under `peerDependenciesMeta` with `optional: true`, with no matching `peerDependencies` entry, so `npm install`/`npm ci` never installs them.** Confirmed via `node -e "console.log(JSON.stringify(require('./package.json').dependencies))"` → `{}`, and `node -e "...peerDependencies..."` → `{}`; `package.json:86-93` only has `peerDependenciesMeta`. Not present in resolved packages in `package-lock.json` (only a transitive sub-dependency, `jinja`, exists under `node_modules/@huggingface/`). The reference `deploy/aml/Dockerfile:18` (`RUN npm ci --ignore-scripts`) and `:26` (`RUN npm ci --omit=dev --ignore-scripts`) do not add it either — **so the AML container image as committed has no local embedding capability**, directly contradicting its own header comment ("Embeddings run on the LOCAL provider (@huggingface/transformers)", `deploy/aml/Dockerfile:7-11`) and its README ("Embeddings: the local default (`@huggingface/transformers`), no API key" — `deploy/aml/README.md:33`). | `package.json:86-93`; `package-lock.json:1-33`; `deploy/aml/Dockerfile:7-11,18,26`; `deploy/aml/README.md:33` | **blocker** | Add an explicit `RUN npm install @huggingface/transformers` (pinned version) to the image build stage, or move it into a real `dependencies` entry for the server image variant. This is a real, currently-shipping gap in the only container reference in the repo, not just a K8s concern. |
| Cache dir + offline mode both gate on one env var: `if (process.env.HIPPO_MODEL_CACHE) { mod.env.cacheDir = process.env.HIPPO_MODEL_CACHE; mod.env.localModelPath = process.env.HIPPO_MODEL_CACHE; mod.env.allowRemoteModels = false; }` — offline mode (`allowRemoteModels = false`) is **only** set when `HIPPO_MODEL_CACHE` is set; there is no separate always-on offline flag. If the var is set but the model isn't pre-baked at that path, load fails and is swallowed (`try { ... } catch { return null; }`, `src/embeddings.ts:219-227`), silently disabling embeddings for that pod rather than erroring loudly. | `src/embeddings.ts:198-203, 219-227` | blocker | Set `HIPPO_MODEL_CACHE` to a mounted, writable path in every manifest, and pre-bake the model into the image at that exact path during build (see `scripts/fetch_embedding_model.mjs`, which already downloads the ONNX bundle to `process.env.HIPPO_MODEL_CACHE` — `scripts/fetch_embedding_model.mjs:109-111`). Run it as a Dockerfile `RUN` step so `allowRemoteModels=false` never hits a missing-model case at pod start. |
| No default cache dir is set anywhere in `src/` if `HIPPO_MODEL_CACHE` is unset — falls through to the transformers.js library's own default (a `.cache/` dir relative to cwd). Given `deploy/aml/Dockerfile:33` sets `WORKDIR /data`, an unset `HIPPO_MODEL_CACHE` would try to write under `/data/.cache` on first download. | `src/embeddings.ts:198` (the `if` gate has no `else` setting a default) | blocker | Never rely on the library default in K8s: a `readOnlyRootFilesystem: true` securityContext (common cluster policy) plus no explicit `HIPPO_MODEL_CACHE`-mounted volume breaks first-run download with no clear error. |
| Embedding calls are fire-and-forget on the write path, not on the HTTP response's critical path: `void embedMemory(globalRoot, globalEntry).catch(() => {});` (`src/shared.ts:94`); `if (isEmbeddingConfigured(hippoRoot)) { embeds.push(embedMemory(hippoRoot, entry).catch(() => {})); }` (`src/capture.ts:552-554`) | `src/shared.ts:94, 420`; `src/capture.ts:552-554` | informational | Confirms no request-time latency block; a cold pod still spends background CPU/network on first model load concurrently with early traffic — call out in a startup/readiness note if pre-baking isn't done. |
| Footprint documentation is inconsistent between two comments for the same model: "~22MB model" vs. "~80 MB tarball" — no RAM figure anywhere in README/ROADMAP/CHANGELOG/docs/. `* Uses @huggingface/transformers (local, zero API keys, ~22MB model).` (`src/embeddings.ts:3`) vs. `Xenova/all-MiniLM-L6-v2 (384-dim, mean pooling, FP32 model.onnx, ~80 MB tarball)` (`scripts/fetch_embedding_model.mjs:15`) | `src/embeddings.ts:3`; `scripts/fetch_embedding_model.mjs:15` | nice-to-have | Reconcile the two numbers (80MB tarball vs. unpacked/RSS at runtime are different things) and measure actual container RSS after first embed before writing a sizing doc — don't guess. |
| A non-local embedding path already exists and sidesteps the whole model/cache/offline problem: `EmbeddingProviderKind = 'local' \| 'openai' \| 'voyage' \| 'cohere'` with an API-key-driven `ApiEmbeddingProvider`. | `src/embedding-provider.ts:43-49, 214-311, 367-401` | nice-to-have | For a first K8s cut, consider defaulting the chart's `embeddings.provider` to an API provider (key from a K8s `Secret`) rather than solving local-model caching/offline immediately — only worth the local-model work if the deployment must stay zero-external-dependency (matches AML's stated intent, but AML doesn't actually ship it today per the row above). |

## 5. Config, secrets, logging

| finding | file:line | severity | suggested change |
|---|---|---|---|
| Unauthenticated loopback fallback grants **admin** role to any process that can reach `127.0.0.1` on the pod — every container in the same Pod, and `kubectl exec` — unless `HIPPO_REQUIRE_AUTH=1`. `// No Authorization header. Loopback-only fallback, unless explicitly disabled via HIPPO_REQUIRE_AUTH=1 ... if (process.env.HIPPO_REQUIRE_AUTH === '1') { throw new HttpError(401, 'auth required'); } assertLocalCaller(req); // v1.12.0: loopback fallback is process-local, treat as admin. return { ... actor: { subject: 'localhost:cli', role: 'admin' } };` | `src/server.ts:645-658` | blocker | Chart must set `HIPPO_REQUIRE_AUTH=1` unconditionally, not as an optional hardening flag. (Binding non-loopback already forces this at boot per section 1's first row — this is the same requirement stated from the auth side.) |
| No way to seed a fixed/known admin key via env var or file at container start. Key minting is CLI-only and non-interactive-capable (`hippo auth create --label ... --role ... --json`, `src/cli.ts:8354-8383`), but always mints a fresh random secret: `createApiKey` (`src/auth.ts:55-67`) has no "adopt this value" input path. | `src/cli.ts:8354-8383`; `src/auth.ts:55-67` | fix before chart | Add a Helm post-install Job running `hippo auth create --role member --json`, capturing stdout into a K8s `Secret` (never logging the plaintext), for the operator's automated bootstrap flow. |
| New API keys default to `admin` role unless `--role member` is passed explicitly: `opts.role ?? 'admin'` (`src/auth.ts:66`), same pattern at `src/api.ts:2192`, migration default also `'admin'` (`src/db.ts:895, 926`). | `src/auth.ts:66`; `src/api.ts:2192`; `src/db.ts:895, 926` | nice-to-have | Bootstrap job should always pass `--role member` explicitly for any non-operator caller; reserve default `admin` only for a human operator key. |
| `HIPPO_HOME` (store root, holding the sqlite DB, API keys, audit log) defaults to `~/.hippo` inside the container's ephemeral filesystem if unset. | `src/project-identity.ts:196-202` | blocker | Same requirement as the storage section: chart must set `HIPPO_HOME` (or the equivalent `hippoRoot`) to a PVC-mounted path, or keys/audit/memory vanish on every pod restart. |
| No per-request access logging in `serve` at all — no line logs method/path/status/tenant/actor for any HTTP request. Only startup/shutdown logging exists, plain text to stderr: `console.error(\`Received ${signal}, shutting down...\`);` (`3489`), `console.error('Error during stop:', err);` (`3493`). No structured logger (pino/winston/bunyan) anywhere in `src/`. | `src/server.ts:3489, 3493` (presence); absence confirmed by grep of the request-handling path | fix before chart | Add one structured JSON line per request (method, path, status, tenant, latency) to stdout — needed for K8s log-based alerting and an audit trail beyond the DB's own `audit_log` table. |
| A file-based log path exists but is unrelated to `serve`: Claude Code session-hook output (`hippo sleep`/`hippo pre-compact` CLI hooks) writes to `~/.hippo/logs/*.log`, not stdout. | `src/hooks.ts:220-232` | nice-to-have | Note in chart docs this path is separate from `serve`'s stdout logging and won't be collected by the container log driver if anyone assumes otherwise. |
| `HIPPO_CLIENT_IP_HEADER` must be set correctly behind a K8s Service/Ingress, or every client routed through the Service's ClusterIP collapses into one shared rate-limit bucket (`HIPPO_V1_RPS`, in-memory, per-process — see section 7). | `src/server.ts:590-609` | fix before chart | Set `HIPPO_CLIENT_IP_HEADER` to whatever the ingress controller stamps (e.g. `x-forwarded-for`) — only if that header is guaranteed set by a trusted proxy layer, per the code's own comment caveat. |
| No hardcoded secret *value* fallback found anywhere (no `?? 'admin-secret-123'` pattern). | grep across `src/` for suspicious default patterns — none found | informational | No action needed. |
| Full env var inventory (plain config vs. secret-shaped) was catalogued by the sub-agent; representative entries: `HIPPO_PORT` (`server.ts:3352`), `HIPPO_HOME` (`project-identity.ts:197`), `HIPPO_REQUIRE_AUTH` (`server.ts:648`), `HIPPO_API_KEY` (`api.ts:385`, client-side bearer), `ANTHROPIC_API_KEY` (`cli.ts:990`), `TYPESAFE_API_KEY` (`judgment.ts:77`), `SLACK_BOT_TOKEN`/`SLACK_SIGNING_SECRET` (`cli.ts:8962`, `server.ts:2705`), `GITHUB_TOKEN`/`GITHUB_WEBHOOK_SECRET` (`connectors/github/cli-impl.ts:98,205-206`). | see above | informational | Any of the connector secrets (`ANTHROPIC_API_KEY`, `SLACK_*`, `GITHUB_*`, `TYPESAFE_API_KEY`) that the deployment actually uses should come from K8s `Secret` objects mounted as env vars, never baked into the image or a ConfigMap. |

## 6. Container (`deploy/aml/`)

The only container reference in the repo. Built for Fly.io, reusable as a starting point for a K8s image.

| finding | file:line | severity | suggested change |
|---|---|---|---|
| Base image `node:22-slim` for both build and run stages; multi-stage build keeps the final image to `dist/`+`bin/`+production `node_modules` only. `FROM node:22-slim AS build` (`Dockerfile:13`); `FROM node:22-slim` (`Dockerfile:22`); `RUN npm ci --omit=dev --ignore-scripts` (`Dockerfile:26`); `COPY --from=build /app/dist ./dist` / `COPY --from=build /app/bin ./bin` (`Dockerfile:27-28`) | `deploy/aml/Dockerfile:13,22,26-28` | informational | Reasonable size discipline already. Main size driver going forward is whether the embedding model gets baked in (tens to ~100+MB, see section 4). |
| **Container has no `USER` directive — runs as root by default.** Privilege drop happens only at runtime in the entrypoint via `setpriv`, gated on `id -u = 0`, specifically because the Fly volume mounts root-owned and needs a one-time `chown`: `if [ "$(id -u)" = "0" ]; then chown node:node /data; exec setpriv --reuid node --regid node --init-groups "$0" "$@"; fi` | `deploy/aml/entrypoint.sh:18-21` | **blocker (for clusters enforcing `runAsNonRoot`)** | Many K8s clusters enforce `securityContext.runAsNonRoot: true` (Pod Security Admission "restricted" or org policy) — the container would be rejected before this script's own chown-then-drop logic ever runs, or would break because it can't chown. Options: (a) pre-chown the PVC via a privileged `initContainer` and run the main container as non-root with `fsGroup` set so the volume is already group-writable, avoiding the need to run the main container as root at all; (b) if root is unavoidable, exclude the pod from `runAsNonRoot` policy via an explicit exception. |
| Only `/data` is written to at runtime (the mounted volume + the entrypoint's own chown of that one path); `/app` (code) is not written to during normal operation. Embedding model cache location is not yet pinned (see section 4) — if `HIPPO_MODEL_CACHE` isn't set, first-run tries to write under the container's cwd (`/data`), not `/app`. | `deploy/aml/Dockerfile:33` (`WORKDIR /data`); `entrypoint.sh:27` (`cd /data`) | fix before chart | Once `HIPPO_MODEL_CACHE` is pinned to a subpath of `/data` (the mounted volume), `readOnlyRootFilesystem: true` should be achievable for `/app` — verify no other write happens outside `/data` (e.g. temp files, npm cache) before flipping that securityContext flag on. |
| `docker-compose.yml` intentionally publishes no port for hippo itself — only an adapter container is reachable, over loopback, fronted by a Cloudflare Tunnel: "Only the adapter is reachable from the host, and only on loopback; the Cloudflare Tunnel points at it. hippo itself has no published ports" | `deploy/aml/docker-compose.yml:1-7` | informational | This is a Fly/Compose-specific topology (adapter pattern for a specific protocol), not directly portable to K8s — a chart would expose `hippo serve` via a K8s `Service` directly (with `HIPPO_REQUIRE_AUTH=1` per section 5), no adapter needed unless the same protocol-translation requirement applies. |
| `HIPPO_REQUIRE_AUTH=1` and `HIPPO_PORT=8080` are already set correctly as image-level `ENV` defaults, matching section 1/5's requirements: `ENV HIPPO_REQUIRE_AUTH=1` / `ENV HIPPO_PORT=8080` | `deploy/aml/Dockerfile:34-35` | informational | Good precedent to carry into the chart's default `values.yaml`. |
| Entrypoint already does the `init`-before-`serve` and `--host 0.0.0.0` steps a chart needs (see section 1's second row): `node /app/dist/cli.js init --no-hooks --no-schedule --no-learn` then `exec node /app/dist/cli.js serve --host 0.0.0.0 --port "${HIPPO_PORT:-8080}"` | `deploy/aml/entrypoint.sh:31,35` | informational | Reuse this exact sequencing as the chart's container entrypoint/command; it already encodes two of the K8s-required fixes from section 1. |
| API key bootstrap is manual/out-of-band by design for this deployment: "API keys are minted OUT OF BAND by the operator: `fly ssh console ... -C "node /app/dist/cli.js auth create --label aml-eval --role member --json"`" | `deploy/aml/entrypoint.sh:7-10` | informational | Matches section 5's finding that there's no env-seeded key path — confirms a Helm post-install Job (or `kubectl exec` once) is the intended pattern, not a code gap specific to K8s. |

## 7. Multi-replica assumptions / Postgres backend (A6/EI10)

| finding | file:line | severity | suggested change |
|---|---|---|---|
| Rate limiter state is a plain in-memory `Map`, built once per `serve()` call: `const buckets = new Map<string, Bucket>();` inside `createRateLimiter`, instantiated from `HIPPO_V1_RPS` at boot (`server.ts:3383`). | `src/rate-limit.ts:40-42`; `src/server.ts:3379-3387` | fix before chart / blocker if scaled | Single-process-only. A sidecar-per-agent-pod topology (1 hippo per pod) is unaffected. If `hippo serve` is ever run as a shared multi-replica Deployment behind a Service, each replica gets an independent bucket — the effective per-client rate limit becomes `N × HIPPO_V1_RPS`, not the configured value. Document this scaling caveat or move rate-limit state to Redis/Postgres before scaling `serve` past 1 replica. |
| pidfile-based "already running" lock (section 1) is inherently per-`hippoRoot`/per-volume, i.e. per-replica-if-each-has-its-own-volume, or a hard single-writer lock if replicas share one volume. | `src/server.ts:3362-3372`; `src/server-detect.ts` | informational | Fine for sidecar (1:1 pod:hippo, own volume per pod). Blocks a shared-volume multi-replica Deployment by design — that topology needs the Postgres backend (below), not a workaround here. |
| SQLite is a hard single-writer store — `index.json` is fully rebuilt from the DB on every write (`writeIndexMirror(hippoRoot, buildIndexFromDb(db))`, called from multiple write paths e.g. `src/store.ts:1515,1555,1720,2071,2263`), and the ROADMAP itself names this as the reason multi-replica/shared-server deployment needs a different backend: "**SQLite allows one writer at a time:** fine per developer; for a company-wide server it is why A6 (Postgres) and EI10 exist." | `src/store.ts:1479` (`writeIndexMirror` definition), `1515,1555,1720,2071,2263` (call sites); `ROADMAP.md:1608` | informational | Confirms today's architecture is scoped to single-writer (one `hippo serve` process per store root) by design, not by oversight. Multi-replica `serve` behind one Service, or a shared cross-tenant server, is out of scope until A6 ships. |
| A6 (Postgres backend) is **planned, not built**: `--db postgres://...` does not exist in the code (no such flag/env found in `cli.ts`/`server.ts` during this audit; ROADMAP marks it "[planned]"). "### A6. Postgres backend [planned] — For shared deployments only. SQLite stays the local default. **Effort:** 3-4w. **Success:** `--db postgres://...` boots; eval suites pass; concurrent-write smoke test green." | `ROADMAP.md:233-235` | informational | A6 is the structural fix for real multi-replica `serve`. Until it ships, the only sound K8s topology is **one hippo process per store root** — i.e. sidecar-next-to-an-agent-pod (matches the brief's stated use case) or single-replica Deployment per tenant/store, never a scaled-out shared Deployment writing one SQLite file. |
| Packaging (Helm, Terraform) is scoped together with Postgres and TLS/encryption under EI10, explicitly "[planned, 6-10w]," i.e. none of it exists yet: "Single-tenant or customer-VPC (**Helm**, Terraform, **Postgres per A6**), fully air-gapped (local embeddings, customer model endpoint, no telemetry) ... TLS, per-key quotas, encryption at rest (A4), plus the central server's backup, restore, high availability and upgrade runbooks" | `ROADMAP.md:1331-1332` | informational | Confirms this audit is genuinely greenfield work, not "find the chart and fix bugs in it." Recommend scoping a first Helm chart to the sidecar/single-replica topology only (matches the brief), and treating "shared multi-tenant server on Postgres" as the separate, already-planned A6/EI10 project. |
