### Documentation

- **A pre-registered test of which lifecycle mechanisms help recall.** `docs/evals/2026-09-23-mechanism-audit-prereg.md` fixes the lanes, the pass and fail rule, and what each verdict changes before any run: physics scoring, the 7-day decay default, outcome feedback, recall strengthening and sleep, each against its own off switch. It runs on local CPU only, through `scripts/e1-lifecycle/compare.mjs`, `benchmarks/longmemeval/paired_hits.mjs` and `benchmarks/longmemeval/build_mech_stores.sh`.
