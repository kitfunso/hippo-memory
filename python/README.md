# hippo-memory (Python SDK)

Async Python SDK for [hippo-memory](https://github.com/kitfunso/hippo-memory), a biologically-inspired memory system for AI agents. Decay by default, strength through use, sleep consolidation.

This package is a thin async HTTP wrapper around the hippo-memory server. Run `hippo serve` from the npm package alongside, then use this SDK to talk to it from Python.

(Full README content lands in Task 7. This stub exists so pyproject.toml's `readme = "README.md"` doesn't error at build time.)
